# Graphify Query Note

- Query attempted against `graphify-out/graph.json`.
- Runtime returned schema mismatch: expected `links` key.
- Fallback applied per run policy: `graphify save-result` + local normalized artifact `graphify-refined.json`.
