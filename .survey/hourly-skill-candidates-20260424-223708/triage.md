# Triage
- Problem: Hourly survey runs need stricter evidence-quality handling when keyword lanes return sparse/noisy results, especially for lane-level keep thresholds.
- Audience: Maintainers operating akillness/oh-my-skills hourly PR maintenance loop.
- Why now: This run required recovery queries in multiple lanes; documenting a deterministic lane-quality threshold prevents weak candidates from entering recommendations.

Provenance: browser-rendered retrieval (gh search repos), direct page retrieval (GitHub repo metadata).
