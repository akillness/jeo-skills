# Triage
- Problem: `codebase-search` is still a high-visibility oversized front door in the search-analysis lane, and its current shape explains many search options without sharply routing users toward one search packet and one next handoff.
- Audience: Agents and maintainers working across CLI/dev workflow, web/fullstack repos, product/ops infrastructure repos, marketing/content repos, and game-development projects who need fast repository navigation before debugging, refactoring, review, or graph mapping.
- Why now: `graphify-out/GRAPH_REPORT.md` still flags `codebase-search` on the oversized front-door watchlist, while adjacent skills (`debugging`, `code-refactoring`, `code-review`, `graphify`, `pattern-detection`) already have sharper packet-first or boundary-first ownership.
