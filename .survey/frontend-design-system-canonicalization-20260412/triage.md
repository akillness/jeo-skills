# Triage
- Problem: `design-system` and `frontend-design-system` currently behave like duplicate frontend catalog entries, which creates trigger ambiguity and maintenance noise.
- Audience: repo maintainers, agent runtimes choosing skills from metadata, and frontend users trying to find the right UI-system skill.
- Why now: the React pair was already canonicalized this run cycle, graph/wiki memory explicitly flagged the design-system pair as the next likely consolidation target, and the current docs/setup surfaces still expose both skills as false peers.
