# Triage
- Problem: The canonical `react-best-practices` skill is highly visible in the frontend lane but still relies on one giant `AGENTS.md` instead of a reusable `references/` bundle, which weakens route clarity and day-to-day reuse.
- Audience: Agents and developers using `oh-my-skills` for React / Next.js performance audits, especially teams diagnosing waterfalls, bundle bloat, hydration issues, RSC boundaries, and rerender churn.
- Why now: The graph and wiki show that support-gap cleanup is down to a small residue; upgrading a canonical frontend anchor is higher leverage than polishing another alias-only wrapper.
