# Triage
- Problem: `lmstudio-cli` is still a thin command-focused skill with no `references/`, no `evals/`, no helper scripts, and weak route-out guidance for local-vs-remote LM Studio workflows.
- Audience: Agents and operators who need to verify LM Studio CLI/server state, load/select models safely, and wire LM Studio into downstream tools that expect OpenAI-compatible endpoints.
- Why now: The repo has been ratcheting high-utility legacy skills into decision-first, support-backed anchors; `lmstudio-cli` is still one of the visible support-gap holdouts in the CLI / developer-workflow lane.
