# Triage
- Problem: Tighten the `state-management` skill further so it acts as a faster packet-first ownership router instead of a still-dense React state architecture explainer.
- Audience: Agents and developers choosing frontend/fullstack state ownership across local UI state, URL/form state, server caches, and client workflow state.
- Why now: Graph/watchlist evidence still flags `state-management` as an oversized frontend front door, while the surrounding frontend cluster is already structurally stronger and support coverage is complete.
