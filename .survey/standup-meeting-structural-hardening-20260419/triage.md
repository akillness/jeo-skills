# Triage
- Problem: `standup-meeting` is still a relatively dense front-door PM skill. It already has the right boundary, but it has not yet been shrunk into the repo’s newer routing-first / packet-first shape.
- Audience: Maintainers of `oh-my-skills` and agents/users who need daily standup / async check-in / blocker-routing guidance without overlapping `task-planning`, `task-estimation`, or `sprint-retrospective`.
- Why now: The PM cluster has already hardened `task-planning`, `task-estimation`, and `sprint-retrospective`. `standup-meeting` is now the obvious remaining daily-coordination anchor to tighten so the cluster stays consistent and discovery surfaces remain honest.
