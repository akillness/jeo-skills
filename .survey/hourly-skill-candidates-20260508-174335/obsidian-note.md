# Obsidian Record (fallback write)

Provenance: thin evidence

- Obsidian CLI was not invoked in this headless cron environment.
- Run artifacts were persisted under `.survey/$SLUG/` for deterministic replay.
