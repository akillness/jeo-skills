| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `.agent-skills/survey/SKILL.md` | One more front-door compaction pass plus a portable validator script will improve transferability more than another wording-only tweak or wrapper skill. | Moved verbose output templates into `references/output-templates-and-validator.md`, added `scripts/validate_survey_artifacts.py`, refreshed evals/discovery surfaces, and validated the survey artifact folder. | `validate_skill.sh` pass; survey artifact validator pass with `--platform-topic`; `validate_catalog_sync.py --repo-root .` pass; JSON parse checks for `survey/evals/evals.json`, `.agent-skills/skills.json`, and `graphify-out/graph.json` pass. | Keep | The skill dropped from 309 to 245 lines, scripts coverage increased, and `graphify-out/GRAPH_REPORT.md` no longer lists `survey` in the top-10 oversized watchlist. |