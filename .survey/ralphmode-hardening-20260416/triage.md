# Triage
- Problem: `ralphmode` is already the repo's cross-platform permission-profile skill, but it still lacks eval coverage and could state the portable settings / rules / hooks model more explicitly for Claude Code, Codex CLI, and Gemini CLI.
- Audience: AI coding-agent operators who want lower approval friction for trusted repos without collapsing into unsafe global bypass, especially users running `ralph` / `jeo`-style long-running loops.
- Why now: the latest graph refresh still shows support-gap work as the highest-leverage maintenance path, and `ralphmode` is one of the remaining high-visibility orchestration skills without `evals/evals.json`.
