# Triage
- Problem: `plannotator` is a high-visibility legacy skill that still lacks `references/` and `evals/`, runs 600 lines, and blurs plan gating, diff review, note export, and platform setup into one long script-like document.
- Audience: AI coding agent users and maintainers working across Claude Code, Codex CLI, Gemini CLI, OpenCode, and orchestrators like `jeo`/`vibe-kanban`.
- Why now: `plannotator` is heavily indexed in discovery docs and upstream orchestration flows, so sharpening its trigger quality and boundaries improves a core review surface used before implementation starts.
