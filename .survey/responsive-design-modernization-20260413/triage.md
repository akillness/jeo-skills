# Triage
- Problem: Decide whether `responsive-design` is the highest-value next frontend modernization target, and if so what its boundary should be relative to `ui-component-patterns`, `web-accessibility`, `web-design-guidelines`, and `design-system`.
- Audience: Agents and maintainers using `oh-my-skills/.agent-skills` for practical frontend implementation, review, and architecture work.
- Why now: `responsive-design` is still a long legacy skill with weak trigger wording and no `references/` or `evals/`, while the adjacent frontend cluster was recently sharpened and the merged `ui-component-patterns` PR clarified the remaining gap.
