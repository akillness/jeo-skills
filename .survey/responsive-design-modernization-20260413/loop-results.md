| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | responsive-design | Rewriting `responsive-design` as a layout-adaptation + verification anchor will deliver clearer trigger boundaries and more reusable guidance than keeping the legacy CSS example dump | Survey + charter + rewrite + support files + docs updates | Passed `validate_skill.sh`; JSON checks passed for `skills.json` and `evals/evals.json` | Keep | Clearer route-outs to `ui-component-patterns`, `web-accessibility`, `design-system`, and `web-design-guidelines` |
