# Triage
- Problem: `backend-testing` is a legacy, overlong skill with weak trigger guidance, little boundary control, and no support files despite covering a high-frequency backend workflow.
- Audience: Agents and developers working on backend APIs, services, auth flows, data-access layers, and regression prevention.
- Why now: The repo already modernized adjacent PM/frontend/marketing/environment skills, and backend-testing is still an 800+ line legacy holdout that likely overlaps awkwardly with `testing-strategies`, `debugging`, `api-design`, and `authentication-setup`.
