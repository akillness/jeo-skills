# Triage
- Problem: Remaining compact discovery drift — 8 skills still lack `SKILL.toon`/`SKILL.compact.md` even though repo guidance, graph memory, and standardization policy treat compact variants as first-class discovery surfaces.
- Audience: `oh-my-skills` maintainers and any agent/runtime that relies on compact skill discovery before loading full `SKILL.md` files.
- Why now: The gap is small, measurable, and directly affects runtime discoverability; a bounded sweep can eliminate the remaining drift without adding new overlapping skills.
