# Triage
- Problem: `design-system` is the canonical frontend UI-system anchor, but its 459-line front door still behaves like an older example dump instead of a fast router with focused reference packets and explicit route-outs.
- Audience: repo maintainers, agent runtimes choosing frontend skills from metadata, and frontend/product engineers who need reusable token/governance/page-system guidance.
- Why now: the alias question was already solved, neighboring frontend anchors were recently hardened into shorter packet-driven skills, graph memory now flags dense high-visibility anchors as the best bounded maintenance target, and `design-system` still contains stale/weakly separated guidance.
