| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `skill-query-handler.py` | Filesystem-first discovery will stop stale manifests from hiding live skills without needing a full manifest redesign. | Load filesystem catalog first, then overlay manifest metadata for matching names only. | `python3 .agent-skills/skill-query-handler.py list --json` now includes `bmad`, `clawteam`, and the 5 game specialist skills. | Keep | Transferable runtime guardrail. |
| 2 | `skill-standardization/SKILL.md` + support files | Standardization work should explicitly include catalog/discovery sync, not just per-skill SKILL.md linting. | Added catalog-sync guidance, `references/catalog-sync-checklist.md`, `scripts/validate_catalog_sync.py`, and a new eval case. | `validate_skill.sh` passes; new sync validator reports no drift after the repair. | Keep | Strengthens future maintenance loops. |
| 3 | `skills.json` + README/setup surfaces | Fixing membership/count drift and surfacing the game-development lane in top-level inventories will reduce hidden-skill risk for future runs. | Repaired manifest membership/counts/paths, added game-development category, updated README / README.ko / setup inventory counts and rows. | `validate_catalog_sync.py --repo-root .` reports no drift; manifest paths resolve. | Keep | Repo-level discovery now matches live folders. |
