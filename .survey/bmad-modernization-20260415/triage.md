# Triage
- Problem: `bmad` is a stale, command-dense BMAD entrypoint with weak trigger wording, legacy naming drift, and too much platform/review/module detail inside one SKILL surface.
- Audience: AI coding-agent users who want BMAD/BMM-style phased orchestration across Claude, Codex, Gemini, and adjacent repo workflows; plus maintainers who need a lower-drift core skill.
- Why now: the repo has already modernized adjacent orchestration anchors (`ohmg`, `bmad-gds`, `task-planning`, `plannotator`), so `bmad` now stands out as a legacy core skill without standard support coverage or clear routing boundaries.
