# Triage
- Problem: `npm-git-install` is still a dense front door compared with newer packet-first skills, so its role is correct but its intake contract and route-outs can be tighter.
- Audience: Agents and maintainers handling Node package delivery choices across CLI/developer workflow, web/fullstack apps, product/ops automation repos, marketing/content stacks, and game tooling projects.
- Why now: Graphify still flags `npm-git-install` as one of the larger utility skills, support coverage is already complete, and the repo's strongest recent pattern is shrinking stable routers instead of adding duplicate wrappers.
