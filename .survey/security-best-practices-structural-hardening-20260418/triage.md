# Triage
- Problem: `security-best-practices` is still an oversized front-door skill and needs a bounded structural-hardening pass so it behaves like a routing-first web/app/API hardening anchor instead of a long mode explainer.
- Audience: Maintainers and agents improving `oh-my-skills`, plus downstream users who need a reusable security hardening brief for frontend, backend, fullstack, marketing-site, and game-service web surfaces.
- Why now: Graph memory still flags it as one of the largest high-degree front doors, support coverage is already complete, and the next highest-value repo-maintenance move is shrinking dense anchors rather than adding another overlapping security wrapper.
