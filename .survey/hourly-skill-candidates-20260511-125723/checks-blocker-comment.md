Hourly carry-forward update pushed, but merge remains blocked.

Blocking reason: no checks reported on this branch.

CI bootstrap needed:
1) Add at least one pull_request workflow under .github/workflows
2) Ensure workflow has repository read permission and runs on this branch
3) Re-run `gh pr checks 271 --watch` and confirm green status

This run added fresh survey artifacts and evidence-contract validator script.
