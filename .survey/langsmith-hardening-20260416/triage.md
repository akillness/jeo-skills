# Triage
- Problem: `langsmith` is still a legacy high-utility skill without eval coverage, and its current body reads like a long feature tour instead of a bounded operator workflow.
- Audience: agents and developers instrumenting, tracing, evaluating, and monitoring LLM applications across Python, TypeScript, LangChain, and custom stacks.
- Why now: support-coverage is down to a small residue of high-value skills without evals, and LangSmith keeps evolving fast enough that weak trigger boundaries and missing validation create avoidable drift in a flagship LLM-ops lane.
