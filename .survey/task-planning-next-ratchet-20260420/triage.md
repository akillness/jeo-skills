# Triage
- Problem: `task-planning` is still one of the repo's highest-degree, oversized front-door skills, so the next maintenance question is whether it needs another bounded hardening pass or should be left alone.
- Audience: Agents and maintainers using `oh-my-skills` for developer workflow, web/fullstack, product/ops, marketing/GTM, and game-planning decomposition work.
- Why now: The latest graph refresh shows support coverage is complete across the catalog, so current value comes from tightening dense routers instead of adding new wrappers.
