# Triage
- Problem: The remaining frontend compatibility aliases (`frontend-design-system` and `vercel-react-best-practices`) still have weak support packets and stale compact discovery text, which makes canonical-vs-alias routing less durable than the rest of the repo.
- Audience: Agents and maintainers using oh-my-skills for frontend design-system and React/Next.js performance work.
- Why now: Graph output shows these are two of the last remaining `references/` holdouts in the repo, and `skills.toon` still advertises both aliases like peer canonical skills instead of narrow compatibility wrappers.
