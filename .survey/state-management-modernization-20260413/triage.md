# Triage
- Problem: The `state-management` skill is still a legacy library-tour skill. It teaches Context / Redux / Zustand examples, but it does not help an agent choose the right state boundary first or separate local UI state, URL state, form state, client global state, and server state.
- Audience: Agents and developers working on frontend/fullstack React apps who need a practical decision workflow instead of a long code dump.
- Why now: The repo has already modernized adjacent frontend and code-quality anchors, while `state-management` still lacks `references/` and `evals/` and is one of the more obvious legacy gaps in the web-development cluster.
