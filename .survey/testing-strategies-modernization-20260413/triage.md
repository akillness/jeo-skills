# Triage
- Problem: `testing-strategies` is a legacy generic skill that still describes a broad test pyramid and sample code, but it lacks a modern mode-selection workflow, support files, and clear boundaries with neighboring skills like `backend-testing`, `test-driven-development`, `code-review`, and `debugging`.
- Audience: Agents and developers using `oh-my-skills` for CLI/dev workflow, web development, backend/fullstack delivery, and product teams that need practical test-planning guidance before implementation or release.
- Why now: Recent upgrades strengthened adjacent code-quality anchors (`backend-testing`, `debugging`, `code-review`), which makes `testing-strategies` the next obvious high-traffic gap and a likely source of overlap or weak routing.
