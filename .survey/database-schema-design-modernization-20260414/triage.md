# Triage
- Problem: `database-schema-design` is still a legacy long-form skill with weak trigger precision, no `references/`, no `evals/`, and too much generic table/SQL example bulk instead of a modern workflow for schema decisions, migration safety, and handoffs.
- Audience: Backend, fullstack, and product engineers who need to design or evolve relational/NoSQL schemas inside normal feature delivery.
- Why now: In the README-visible Backend cluster, `database-schema-design` is now the only backend anchor still missing both support bundles and modernization, while adjacent backend skills (`api-design`, `api-documentation`, `authentication-setup`, `backend-testing`) have already been sharpened.
