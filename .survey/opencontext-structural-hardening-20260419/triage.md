# Triage
- Problem: `opencontext` is still an oversized project-memory front door without a compact intake-packet / route-out reference, and some derived surfaces still carry older command-heavy wording or stale source paths.
- Audience: CLI-first agent users, repo-maintainers, fullstack teams, PM/ops owners, content/marketing operators, and game-production teams who need durable repo/project context across sessions.
- Why now: Graphify’s oversized-front-door watchlist still flags `opencontext`, the persistent-memory cluster already treats it as the active project-memory lane, and the next best maintenance win is tightening that contract instead of adding another memory wrapper.
