# Triage
- Problem: `changelog-maintenance` is still a dense 292-line front door in a documentation cluster that now favors smaller routing-first anchors with packetized support docs.
- Audience: maintainers of `oh-my-skills/.agent-skills`, plus downstream agents that need crisp triggering for changelogs, release notes, migration updates, and lightweight game patch notes.
- Why now: graph and wiki evidence show support coverage is already complete repo-wide, so the highest-value bounded win is shrinking another dense front door instead of adding a new wrapper.
