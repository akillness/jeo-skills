# Triage
- Problem: `testing-strategies` still reads like a broad testing-policy explainer instead of a routing-first front door that starts from the validation packet teams already have.
- Audience: agents and maintainers handling CLI/dev, web/fullstack, and product/release validation decisions before implementation or review.
- Why now: graphify still flags oversized front-door skills as the highest-value maintenance target, and the code-quality lane already depends on `testing-strategies` as the policy router for `backend-testing`, `debugging`, `code-review`, and `performance-optimization`.
