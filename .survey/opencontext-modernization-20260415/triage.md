# Triage
- Problem: `opencontext` is still a command-heavy legacy skill with no `references/` or `evals/`, weak route-outs to adjacent memory layers, and discovery copy that hides when to choose it over `llm-wiki`, Obsidian, or graph-style repo mapping.
- Audience: agents and operators who need durable project/repo memory, cross-session continuity, searchable conclusions, and multi-agent handoff in CLI-centric development workflows.
- Why now: the hourly repo scan across CLI/dev workflow, web/fullstack, PM/ops, marketing/content, and game-development lanes found that the broad memory/context lane remains a high-utility support-gap after the latest utility-skill modernizations; upgrading the existing anchor is higher-value than adding another overlapping memory wrapper.
