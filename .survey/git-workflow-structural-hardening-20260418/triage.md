# Triage
- Problem: identify the next bounded high-value `.agent-skills` improvement after merging the `task-planning` PR, with a bias toward dense front-door skills whose routing can be sharpened without adding catalog noise.
- Audience: maintainers and agent users who rely on `oh-my-skills` discovery surfaces to pick the right reusable workflow skill quickly.
- Why now: graph + wiki memory say support coverage is complete across the live catalog, so the next ratchets should target oversized, high-visibility anchors instead of adding more wrappers.