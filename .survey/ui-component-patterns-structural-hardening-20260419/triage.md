# Triage
- Problem: `ui-component-patterns` is still an oversized frontend router and likely needs a tighter routing-first front door plus clearer support-packet handoffs instead of another overlapping component-architecture skill.
- Audience: agents and maintainers working on reusable UI primitives/components across React, TypeScript, design-system, product UI, and frontend platform repos.
- Why now: graphify still flags `ui-component-patterns` on the oversized front-door watchlist, while frontend support coverage is already complete; the next bounded win should tighten the existing anchor rather than add another wrapper.
