| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `.agent-skills/responsive-design/SKILL.md` | Shrinking the skill into a routing-first responsive packet anchor will improve trigger clarity without losing practical layout guidance | Rewrote the front door around one primary packet, one concise strategy artifact, explicit route-outs, and support-file references | `validate_skill.sh` passed with 0 errors/0 warnings; file reduced to 244 lines; frontend-cluster boundary stayed clear | Keep | Matches the repo's hardened frontend pattern |
| 2 | `references/` + evals + compact/docs surfaces | A small packet router plus synced discovery surfaces will transfer better than adding another frontend wrapper | Added `references/intake-packets-and-route-outs.md`, expanded evals with a launch-readiness boundary case, refreshed `SKILL.toon`, `skills.json`, `skills.toon`, README, README.ko, and setup prompt | JSON parse checks passed for `evals/evals.json`, `.agent-skills/skills.json`, and `graphify-out/graph.json`; refreshed graph output removed `responsive-design` from the oversized watchlist | Keep | The durable win was sync + boundary clarity, not catalog expansion |
