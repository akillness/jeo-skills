# Triage
- Problem: `responsive-design` is still an oversized frontend front door; the likely bounded improvement is shrinking it into a routing-first responsive strategy packet instead of leaving a long mixed guide.
- Audience: frontend and fullstack developers, design-system-adjacent frontend owners, and QA/accessibility reviewers who need to classify responsive layout failures quickly.
- Why now: `graphify-out/GRAPH_REPORT.md` still flags `responsive-design` on the oversized front-door watchlist while adjacent frontend skills (`web-accessibility`, `state-management`, `design-system`) have already been tightened into smaller routing-first anchors.
