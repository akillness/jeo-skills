# Triage
- Problem: `deployment-automation` is still a long legacy CI/CD + Docker + Kubernetes dump without strong trigger boundaries, support files, or modern route-outs, despite being the next clear developer-workflow / web-delivery anchor identified by current graph and wiki notes.
- Audience: Agents and developers who need to design or upgrade delivery pipelines for web/fullstack projects, multi-environment deploy flows, preview/staging/prod promotion, rollback safety, and platform-specific release automation.
- Why now: The repo’s current structural graph still flags deployment automation as a remaining high-visibility legacy anchor lacking both `references/` and `evals/`; sharpening it improves a core developer-workflow lane without creating another overlapping deployment wrapper.
