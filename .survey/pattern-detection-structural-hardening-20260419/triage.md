# Triage
- Problem: `pattern-detection` still reads like a broad mode catalog instead of a tighter routing-first front door for reusable pattern/rule/anomaly hunts.
- Audience: Maintainers of `oh-my-skills` plus agents and users who need one bounded skill for repeated code-pattern scans, log/event clustering, telemetry anomaly triage, and metric outlier detection.
- Why now: The neighboring search-analysis anchors (`codebase-search`, `log-analysis`, `looker-studio-bigquery`, `langsmith`) have already been tightened into packet-first routers, so `pattern-detection` is now the most obvious remaining broad front door in that lane.
