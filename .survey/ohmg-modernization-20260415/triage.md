# Triage
- Problem: The `ohmg` skill is stale against upstream `oh-my-agent` reality and is mispositioned as a Gemini-only Antigravity framework instead of a Gemini/Antigravity-facing adapter for the portable `.agents`-based `oh-my-agent` harness.
- Audience: Gemini CLI users, Antigravity users, and mixed-vendor agent operators choosing between `ohmg`, `omc`, and `omx` inside `oh-my-skills`.
- Why now: `ohmg` still points at old `oh-my-ag` naming and commands, while upstream `first-fluke/oh-my-agent` is active, portable, and materially different; leaving the skill stale creates routing confusion and duplicate-overlap risk with `omc` and `omx`.
