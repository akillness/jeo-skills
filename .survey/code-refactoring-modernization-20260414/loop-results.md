| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `code-refactoring/SKILL.md` | A mode-selecting rewrite with explicit verification loops and route-outs will outperform the legacy pattern dump on trigger clarity and dry-run usefulness | Rewrote the skill around three modes (`local safe refactor`, `behavior freeze first`, `repetitive migration / codemod`), added `references/` + `evals/`, updated discovery surfaces, and refreshed graph/wiki artifacts | `validate_skill.sh` passed with 0 errors / 0 warnings; eval JSON parsed successfully; graph snapshot preserved support-coverage visibility while making the code-quality cluster cleaner | Keep | Transferable improvement across CLI, backend, frontend, fullstack, and game-programming cleanup workflows |
