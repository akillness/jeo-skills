# Triage
- Problem: `code-refactoring` is still a legacy example-heavy refactoring dump with weak trigger precision, no support files, and weak boundaries versus `code-review`, `debugging`, `testing-strategies`, and `performance-optimization`.
- Audience: Developers and coding agents doing behavior-preserving cleanup, decomposition, duplication removal, and repetitive API migrations across CLI, web, backend, fullstack, and game-programming repos.
- Why now: The code-quality cluster has stronger neighboring anchors now, which makes `code-refactoring` the highest-value remaining code-quality modernization target instead of adding another overlapping wrapper.
