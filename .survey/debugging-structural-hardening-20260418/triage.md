# Triage
- Problem: The `debugging` skill is a high-degree anchor that still carries a dense front door; survey how modern debugging guidance separates reproduce/isolate/verify from log triage, test policy, review, and performance work so the skill can be structurally hardened instead of spawning another wrapper.
- Audience: Agents and maintainers using `oh-my-skills` for CLI, backend, frontend, fullstack, CI, and game-development debugging requests.
- Why now: `debugging` is the highest-degree live skill in the graph and one of the remaining oversized front doors, so a bounded routing-first hardening pass can improve trigger precision and reuse across multiple domains.
