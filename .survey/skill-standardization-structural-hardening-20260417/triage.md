# Triage
- Problem: `skill-standardization` is a high-degree maintenance skill whose front door is oversized and still contains repo-path ambiguity (`scripts/validate_skill.sh` examples fail from the repo root), making standardization runs less deterministic than they should be.
- Audience: Maintainers and agents creating, validating, consolidating, or catalog-syncing skills in `oh-my-skills` and similar Agent Skills repositories.
- Why now: The repo's support coverage is already complete, so the next compounding win is sharpening dense anchor skills; `skill-standardization` is on the graph watchlist and directly affects the quality of future skill maintenance loops.
