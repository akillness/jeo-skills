# Triage
- Problem: `security-best-practices` is still a legacy snippet-dump skill even though adjacent modernized skills now route broader hardening work to it; it needs a modern, reusable security-hardening workflow instead of Node/Express-only code examples.
- Audience: Agents and developers working on web apps, APIs, backend/fullstack products, and deployment-adjacent hardening work who need a vendor-neutral security decision aid.
- Why now: The repo now has stronger neighboring skills (`authentication-setup`, `database-schema-design`, `system-environment-setup`, `pattern-detection`), which makes `security-best-practices` the weakest remaining web/fullstack security anchor and a common route target.
