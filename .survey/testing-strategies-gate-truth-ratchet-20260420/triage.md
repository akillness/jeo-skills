# Triage
- Problem: `testing-strategies` still risks blurring branch-blocking checks, release-only proof, and platform-specific launch checklists into one generic validation brief.
- Audience: Agent authors and teams using `testing-strategies` to decide merge gates, release confidence, flaky-suite policy, and cross-domain validation ownership across software, web, ops, and game delivery.
- Why now: The repo graph still flags `testing-strategies` as an oversized front door, and the next durable win is to make merge-gate truth and release handoffs sharper instead of adding another QA or release wrapper.
