| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `.agent-skills/api-design/SKILL.md` | Narrowing `api-design` into a contract-first workflow with explicit handoffs and support files will improve trigger precision and reduce overlap with `api-documentation`. | Rewrote `api-design`, added `references/` + `evals/`, and updated discovery surfaces (`README.md`, `README.ko.md`, `setup-all-skills-prompt.md`, `.agent-skills/skills.json`) | `validate_skill.sh` passed with 0 errors / 0 warnings; JSON validation passed for `evals.json` and `skills.json`; graph/wiki refresh completed cleanly | Keep | Transferable improvement: stronger contract-first boundary, sharper routing to adjacent backend skills, better discovery wording |
