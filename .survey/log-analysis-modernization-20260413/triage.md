# Triage
- Problem: The existing `log-analysis` skill is a legacy grep-centric command dump that does not reflect modern root-cause-first log triage across CLI, web, and game-adjacent workflows.
- Audience: Developers, full-stack engineers, incident responders, platform engineers, and game teams who need fast read-only triage of noisy logs.
- Why now: Graph + wiki review shows `log-analysis` is the only uncovered game-adjacent support gap and still lacks `references/` + `evals/`, while adjacent clusters have already been modernized around clearer boundaries.
