# Triage
- Problem: `bmad-gds` is still a generic phase/command catalog, but the current repo now has a stronger game-skill cluster that supports more practical orchestration across planning, build triage, playtest feedback, performance, and launch prep.
- Audience: Solo developers and small indie game teams using AI to coordinate design, engineering, QA, playtesting, and shipping across Unity, Unreal, Godot, or custom-engine projects.
- Why now: The repo already has stronger adjacent game skills (`game-build-log-triage`, `game-demo-feedback-triage`, `game-performance-profiler`, `steam-store-launch-ops`), so `bmad-gds` is now the weak coordinating layer in the cluster and the best place to upgrade practical leverage instead of adding another overlapping skill.
