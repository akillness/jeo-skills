| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `.agent-skills/langsmith/SKILL.md` | Shrinking the skill into a routing-first packet selector will improve trigger clarity without losing the six-mode LangSmith boundary | Rewrote the front door around one primary packet, one compact brief, explicit route-outs, and support-file references | `validate_skill.sh` passed with 0 errors/0 warnings; file reduced to 262 lines; graph refresh removed `langsmith` from the top-10 oversized watchlist | Keep | Matches the repo's hardened packet-first pattern |
| 2 | `references/` + evals + compact/docs surfaces | A tiny packet chooser reference plus synced eval/discovery wording will transfer better than adding another LLM observability wrapper | Added `references/intake-packets-and-route-outs.md`, expanded evals with a prompt-registry case, refreshed `SKILL.toon`, `skills.json`, `skills.toon`, README, README.ko, and setup prompt | Catalog sync validator passed with no drift; JSON parse checks passed; residue scan found no stale LangSmith phrasing after `skills.toon` sync | Keep | Durable win was boundary clarity + synced discovery surfaces, not catalog growth |
