# Triage
- Problem: `langsmith` is still one of the repo's oversized front-door skills, and its current surface mixes workflow selection, instrumentation choices, evaluator choices, and output shape in the main `SKILL.md` instead of pushing more detail into reusable packets.
- Audience: AI coding agents and operators wiring LangSmith into chat apps, RAG systems, agents, support copilots, prompt review loops, and multi-service AI backends.
- Why now: `graphify-out/GRAPH_REPORT.md` still lists `langsmith` on the oversized watchlist while the surrounding repo has already converged on smaller routing-first anchors with compact discovery surfaces and packet references.
