# Triage
- Problem: `code-review` is a high-degree code-quality anchor with a still-dense front door; the next maintenance question is whether to harden it in place around clearer intake packets and route boundaries instead of adding another review wrapper.
- Audience: Agents and maintainers using `.agent-skills` for code, PR, and diff review across CLI/dev workflow, web/fullstack, product-facing changes, marketing-site changes, and game-programming repos.
- Why now: The graph snapshot shows support coverage is already complete across the live catalog, so the highest-leverage hourly improvements now come from shrinking high-traffic skills and keeping discovery surfaces truthful.
