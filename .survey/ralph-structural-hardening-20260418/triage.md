# Triage
- Problem: `ralph` is still the repo's most oversized front-door skill and its platform guidance is partly stale, especially around Codex-native hooks and runtime ownership.
- Audience: Agents and operators using `ralph` for specification-first clarification and persistent completion across Claude Code, Codex CLI, Gemini CLI, OpenCode, and adjacent repo orchestration flows.
- Why now: Graphify still flags `ralph` as the top oversized front door (499 lines), repo-local docs still advertise Codex as compensation-only despite newer native hooks, and the orchestration cluster already has enough neighboring skills that another wrapper would add noise instead of clarity.
