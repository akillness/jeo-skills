# Triage
- Problem: Decide the highest-value bounded modernization target among the remaining legacy frontend anchors, centered on `responsive-design` and `ui-component-patterns`, while checking boundary overlap with `web-accessibility`, `web-design-guidelines`, `state-management`, and `design-system`.
- Audience: Agents and maintainers using `oh-my-skills/.agent-skills` for practical frontend implementation, review, and architecture work.
- Why now: The graph and wiki show these frontend anchors still have legacy long-form content, weak trigger wording, and no `references/` or `evals/`, while nearby frontend skills were recently sharpened and documented.
