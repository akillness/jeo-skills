# Triage
- Problem: 시간당 루프에서 backlog(PR #157) 우선 처리 후, 5개 필수 키워드(`agentic ai skill`, `web frontend skill`, `web backend skill`, `cli open source skill`, `game development skill`)를 브라우저 검색으로 재검증하고 신규/개선안을 PR 단위로 반영해야 한다.
- Audience: `oh-my-skills` maintainer, 리뷰어, 자동화 운영자.
- Why now: backlog PR이 merge 가능 상태였고, 이번 런에서 신규 스킬(`aider-cli-workflow`) 추가와 기존 스킬 route-out 개선을 함께 반영해 중복/리스크 기준을 다시 점검해야 한다.
