# Triage
- Problem: `workflow-automation` is still a legacy generic automation skill with broad examples, weak trigger boundaries, and no supporting references or evals despite being a high-utility developer-workflow anchor.
- Audience: Agents and developers automating recurring local/CI/release workflows across CLI, web, and fullstack repositories.
- Why now: The repo has recently strengthened several high-traffic legacy anchors with sharper boundaries, support bundles, and better discovery surfaces; `workflow-automation` is still broadly indexed in docs but remains low-signal and overlap-prone.
