## Settings
- Use repository-local validators and deterministic artifact paths in `.survey/<slug>/`.

## Rules
- Apply relevance gate with CLI-negation low-fit handling for recommendation-grade keeps.

## Hooks
- Prefer non-interactive cron-safe commands; capture stderr artifacts for degraded transport.

## Platform Gaps
- web_search API key invalid in this environment; GH API fallback used for discovery.
