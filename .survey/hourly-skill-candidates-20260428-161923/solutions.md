## Solution List
- Improve existing survey skill: add explicit guidance for handling web_search INVALID_API_KEY fallback to gh api with artifact traceability.
- Improve/new obsidian fallback skill: add headless sync troubleshooting subsection (URI failure + direct vault write + status marker).

## Categories
- survey reliability
- obsidian headless automation

## What People Actually Use
- GitHub API repository search as fallback discovery transport in automation contexts.
- Obsidian URI/direct-write split patterns for headless note persistence.

## Frequency Ranking
1. survey transport fallback
2. obsidian URI fallback
3. headless sync operations

## Key Gaps
- Missing single canonical playbook entry for INVALID_API_KEY fallback in hourly runs.

## Contradictions
- None in this run.

## Key Insight
- Carry-forward merge is preferable now because open PR backlog is high; prioritize merging validated pending work over opening another PR.
