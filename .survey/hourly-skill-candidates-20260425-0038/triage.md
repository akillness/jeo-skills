# Triage
- Problem: Hourly repo-maintenance survey results are still noisy and can miss relevant candidates unless keyword-family sweeps and rescue gates are explicit.
- Audience: Maintainers of `akillness/oh-my-skills` running autonomous PR loops for skill updates.
- Why now: This hour's sweep returned sparse/noisy hits (notably empty `web backend skill` and `cli open source skill` results), so guidance should encode mandatory keyword sweep + fallback behavior. (direct page retrieval)
