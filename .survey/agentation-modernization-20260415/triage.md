# Triage
- Problem: `agentation` is a high-visibility planning/review skill but still reads like a sprawling install-and-command catalog instead of a decision-first UI annotation router.
- Audience: Agent users coordinating browser verification, UI feedback, and targeted code-fix loops across Claude Code, Codex, Gemini CLI, OpenCode, and adjacent browser tooling.
- Why now: The open PR queue is clear after merging `opencontext`, `agentation` is the weakest support-coverage outlier in a flagship JEO path, and the repo still has README/setup count drift that should be fixed while touching discovery surfaces.
