| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | `.agent-skills/agent-browser/SKILL.md` | Reframing `agent-browser` as the clean-session verification anchor with explicit route-outs and evals will improve trigger precision and cluster clarity | Rewrote the skill around fresh-session verification, observe → act → observe, route-outs to `playwriter` / `agentation` / `plannotator`, added `references/modes-and-routing.md`, added `evals/evals.json`, and synced discovery surfaces | `validate_skill.sh` passed, eval JSON parsed, and `validate_catalog_sync.py --repo-root .` reported no drift | Keep | Transferable browser-review rule: separate clean-session verification from running-browser reuse and rendered-UI feedback |
