# Triage
- Problem: `agent-browser` is high-traffic in the repo's browser-review lane but still reads like a command-centric browser CLI guide, lacks evals, and needs sharper routing around fresh-session deterministic verification versus running-browser reuse and rendered-UI feedback.
- Audience: AI coding-agent operators, web developers, QA/verification loops, and repo maintainers who need reproducible browser checks inside Claude/Codex/Gemini/OpenCode workflows.
- Why now: `playwriter` and `agentation` were just modernized, the browser-review cluster boundary is clearer, and `agent-browser` is now the main remaining high-visibility browser skill without eval coverage.
