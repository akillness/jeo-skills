# Triage
- Problem: The repo currently has two overlapping developer-environment skills: `environment-setup` mixes `.env` / app config guidance, while `system-environment-setup` mixes broader reproducible local environment guidance. Both are legacy, overlapping, and weakly separated.
- Audience: AI coding agents and maintainers who need reliable guidance for project bootstrap, local runtime parity, secrets/config handoff, onboarding, and environment troubleshooting.
- Why now: The environment/setup pair is still a blurry duplicate cluster in a high-utility developer-workflow lane, both entries are legacy-quality, and the repo now has a repeated pattern of upgrading one canonical anchor plus a narrower compatibility alias instead of keeping vague peer duplicates.
