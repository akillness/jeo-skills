# Triage
- Problem: `jeo` is still a dense front-door orchestration skill whose trigger surface, packet boundaries, and platform/runtime route-outs can be made sharper without creating a duplicate orchestration wrapper.
- Audience: Agent operators and repo maintainers using Claude Code, Codex CLI, Gemini CLI, or OpenCode to run plan → execute → verify workflows with durable project memory.
- Why now: Graph and wiki memory still flag `jeo` as one of the largest front-door skills in the catalog, and the repo currently has no open PRs, so this is a good bounded follow-up after merging the backend-testing hardening PR.
