# Triage
- Problem: `code-refactoring` is still an oversized front-door skill with stale compact/discovery surfaces (`SKILL.toon`, `skills.toon`) that over-advertise generic design-pattern cleanup instead of the repo's current behavior-preserving cleanup boundary.
- Audience: Agents and maintainers using `oh-my-skills` for CLI/dev workflow, web/fullstack, backend, and game-programming code cleanup without changing intended behavior.
- Why now: `graphify-out/GRAPH_REPORT.md` still flags `code-refactoring` on the oversized watchlist, and stale discovery surfaces risk mis-triggering or hiding the sharper routing boundary created during modernization.
