| Iteration | Target | Hypothesis | Change | Validation result | Keep/Revert | Notes |
|---|---|---|---|---|---|---|
| 1 | survey artifacts | A survey-first pass will show whether the highest-leverage improvement is a new research skill or a hardening pass on `survey` itself | Wrote `triage.md`, `context.md`, `solutions.md`, and `platform-map.md` for `survey-evidence-recovery-20260412` | Evidence pointed to repeated search/extract failures and ad hoc fallback behavior in prior runs, with `survey` as the right anchor | Keep | Bounded scope stayed inside the existing `survey` skill |
| 2 | `.agent-skills/survey/SKILL.md` | Making the evidence-recovery ladder explicit inside the main skill will improve reuse without adding overlap | Expanded execution policy, added `## Evidence Recovery Ladder`, standardized headings, and improved trigger-oriented description | `validate_skill.sh` passed with 0 errors / 0 warnings after standardization tweaks | Keep | Main skill stayed under 500 lines and remained clearly triggerable |
| 3 | survey support files | Moving detailed fallback patterns into support files will improve transfer without bloating the SKILL | Added `references/evidence-recovery-ladder.md` and `evals/evals.json` covering auth failure, URL drift, and snippet-only recovery | Structural graph refresh now counts `survey` among skills with both `references/` and `evals/` | Keep | Transferable improvement because future runs can reuse the ladder instead of rediscovering it |
| 4 | Discovery + memory surfaces | If `survey`'s role changed materially, discovery surfaces and durable memory should reflect it | Updated README, README.ko, setup prompt, `skills.json`, `skills-lock.json`, refreshed `graphify-out/`, and updated wiki pages/log | Repo state is coherent; no new overlapping skill introduced | Keep | This preserves discoverability and compounding memory for the next hourly loop |
