# Triage
- Problem: Hourly survey runs still produce lane-level degraded outcomes (frontend/backend) and inconsistent root-cause clarity when discovery is sparse/noisy.
- Audience: Maintainers/reviewers of `akillness/oh-my-skills` operating unattended hourly maintenance loops.
- Why now: Current run `hourly-skill-candidates-20260426-070240` shows backend `raw_count=0` and frontend noisy hits; codified reviewer-facing guidance is needed so degraded lanes are actionable instead of ambiguous.

## Evidence Contract
- Provenance labels used in this run: direct page retrieval, indexed snippet, thin evidence.
- Primary retrieval mode: direct page retrieval (GitHub search API).
