N:git-submodule
D:Decide when Git submodules are the right dependency boundary, then operate them safely: add, bootstrap recursively, update pointers, handle detached HEAD, remove cleanly, or configure CI checkout. Use for `.gitmodules`, `git submodule`, recursive clone/setup, pointer updates, private submodules in CI, or submodule-vs-subtree decisions.
G:git submodule subtree dependencies version-control ci repo-structure
U[5]:
  Add an external repository as a pinned submodule
  Bootstrap or clone a repo with existing submodules
  Advance a tracked branch and commit the pointer update
  Work inside a submodule without detached HEAD confusion
  Configure CI checkout for public or private submodules
S[8]{n,action}:
  1,Normalize the submodule request
  2,Decide whether submodule is the right tool
  3,Choose one workflow mode
  4,Apply the decision ladder
  5,Build the submodule brief
  6,Use mode-specific patterns
  7,Keep operator rules visible
  8,Route adjacent problems to the right skill
