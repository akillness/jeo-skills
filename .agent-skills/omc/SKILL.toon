N:omc
D:Route Claude Code–first orchestration to the right OMC surface before giving commands. Distinguishes plugin slash skills (`/team`, `/autopilot`, `/ralph`, `/ultrawork`) from the `omc` shell CLI (`omc setup`, `omc team`, `omc ask`), handles setup/recovery/plugin-dir/state issues, and routes adjacent work to jeo, ralphmode, omx, ohmg, and browser-review skills.
G:omc oh-my-claudecode claude-code /team autopilot ralph ultrawork ccg omc-team omc-ask omc-setup hud hooks
U[5]:
1:Claude Code plugin setup and repair for OMC / oh-my-claudecode
2:Truthful routing between in-session slash skills and shell-side `omc` CLI commands
3:Mode selection for `/team`, `/autopilot`, `/ralph`, `/ultrawork`, and `omc team`
4:Recovery guidance for duplicate installs, plugin-dir confusion, worktree/state drift, and HUD/update issues
5:Explicit route-outs to jeo, ralphmode, omx, ohmg, plannotator, and browser-review skills
S[5]{n,action,details}:
1:classify,Pick one packet,plugin-setup | in-session-runtime | terminal-runtime | recovery-and-update | boundary-and-route-out
2:surface,State the runtime surface,plugin slash skills vs shell-side `omc` CLI
3:recommend,Give the smallest truthful command,/team or /autopilot or /ralph or omc setup or omc team or omc ask depending on packet
4:recover,Call out real operator constraints,enable CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 and inspect plugin-dir/state/worktree behavior when relevant
5:route-out,Hand off non-OMC ownership,jeo / ralphmode / plannotator / agent-browser / playwriter / agentation / omx / ohmg
R[5]:
1:`/team` and `omc team` are both real but they are different runtimes
2:`/autopilot`, `/ralph`, and `/ultrawork` are in-session skills, not normal `omc` CLI subcommands
3:The npm package name is `oh-my-claude-sisyphus` while the runtime command is `omc`
4:Native team mode depends on CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 in ~/.claude/settings.json
5:Use references for volatile operator detail; keep the front door focused on routing
E[3]{desc,in,out}:
1:{Claude Code team mode,/team 3:executor "fix all TypeScript errors",Use the in-session-runtime packet and recommend the native Claude Code team workflow}
2:{Shell-side worker team,omc team 2:codex "review auth flow",Use the terminal-runtime packet and explain tmux worker orchestration}
3:{Long-loop orchestration boundary,I want a resumable plan→execute→verify loop across multiple skills,Route to jeo and mention OMC only as the Claude-first runtime layer}
